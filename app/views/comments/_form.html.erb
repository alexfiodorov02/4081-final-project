<%= form_with(model: [@parent, @comment], url: post_comments_path(@parent), local: true) do |form| %>
  <div class="field">
    <%= form.label :body, 'Comment' %>
    <%= form.text_area :body %>
  </div>
  <div class="actions">
    <%= form.submit "Submit Comment" %>
  </div>
<% end %>
